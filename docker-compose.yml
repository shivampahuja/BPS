version: "3.9"
services:
  cii-webapp:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    volumes:
      - ./app/data:/workspace/app/data
      - ./app/exports:/workspace/app/exports
      - ./app/models:/workspace/app/models
    restart: unless-stopped
